text='''================================================================================
                   Python 字符串方法速查表 (30-Days-Of-Python 第四天)
================================================================================

【核心方法：日常开发必用，建议掌握】
--------------------------------------------------------------------------------
方法                | 作用                          | 示例                     | 结果
--------------------------------------------------------------------------------
.upper()            | 全部转为大写                  | "Hi".upper()             | "HI"
.lower()            | 全部转为小写                  | "Hi".lower()             | "hi"
.strip()            | 去除首尾空格/换行             | " hi ".strip()           | "hi"
.split()            | 按指定字符拆分为列表          | "a,b".split(",")         | ['a','b']
.join()             | 用指定字符合并列表            | ",".join(['a','b'])      | "a,b"
.find()             | 查找子串位置(找不到返回-1)    | "hello".find("e")        | 1
.replace()          | 替换子串                      | "hi".replace("i","ello") | "hello"
.startswith()       | 是否以某子串开头              | "hi".startswith("h")     | True
.endswith()         | 是否以某子串结尾              | "hi".endswith("i")       | True
len()               | 获取字符串长度                | len("hi")                | 2
--------------------------------------------------------------------------------

【常用方法：特定场景下会用到】
--------------------------------------------------------------------------------
方法                | 作用                          | 示例                     | 结果
--------------------------------------------------------------------------------
.capitalize()       | 首字母大写，其余小写          | "python".capitalize()    | "Python"
.title()            | 每个单词首字母大写            | "hello world".title()    | "Hello World"
.swapcase()         | 大小写互换                    | "Hi".swapcase()          | "hI"
.count()            | 统计子串出现次数              | "hi hi".count("hi")      | 2
.index()            | 查找子串位置(找不到报错)      | "hi".index("i")          | 1
.rfind()            | 从右侧开始查找子串位置        | "hello".rfind("l")       | 3
.rindex()           | 从右侧查找(找不到报错)        | "hello".rindex("l")      | 3
.lstrip()           | 去除左侧空格                  | " hi".lstrip()           | "hi"
.rstrip()           | 去除右侧空格                  | "hi ".rstrip()           | "hi"
--------------------------------------------------------------------------------

【判断类方法：返回 True / False】
--------------------------------------------------------------------------------
方法                | 检查内容                      | 示例 (结果为 True)
--------------------------------------------------------------------------------
.isalpha()          | 是否全是字母                  | "Hello".isalpha()
.isdigit()          | 是否全是数字                  | "123".isdigit()
.isalnum()          | 是否全是字母或数字            | "Hello123".isalnum()
.islower()          | 是否全是小写                  | "hello".islower()
.isupper()          | 是否全是大写                  | "HELLO".isupper()
.isspace()          | 是否全是空格                  | "   ".isspace()
.isidentifier()     | 是否是合法变量名              | "name_1".isidentifier()
--------------------------------------------------------------------------------

【进阶方法：了解即可，用到时候再查】
--------------------------------------------------------------------------------
方法                | 作用                          | 示例
--------------------------------------------------------------------------------
.expandtabs()       | 将制表符(\t)替换为空格        | "a\tb".expandtabs(4)
.format()           | 字符串格式化(旧式)            | "{} + {}".format(1,2)
.zfill()            | 用0在左侧填充到指定宽度       | "42".zfill(5) -> "00042"
.center()           | 将字符串居中，用指定字符填充  | "hi".center(5, "-") -> "--hi--"
--------------------------------------------------------------------------------

【使用建议】
1. 收藏此表，需要时查阅，不用背诵。
2. 先想“我要做什么”，再找对应方法。
3. 记不住很正常，这是程序员工作的一部分。

================================================================================'''
print(text)
input("输入回车键结束")
